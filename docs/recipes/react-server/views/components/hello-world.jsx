/**
 * Created by novacrazy on 3/22/2015.
 */

define( ['react'], function(React) {
    return React.createClass( {
        render: function() {
            return (
                <h1>Hello, World!</h1>
            );
        }
    } );
} );
